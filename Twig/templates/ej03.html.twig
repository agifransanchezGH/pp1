<html>
    <body>
        <ul>
            {% for pais in paises %}
                <li>{{ pais.nombre }}</li>
                <li>{{ pais.moneda }}</li>
                <li>{{ pais.lengua }}</li>
            {% endfor %}
        </ul>
    </body>
</html>